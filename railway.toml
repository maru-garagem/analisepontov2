[build]
builder = "DOCKERFILE"

[deploy]
# startCommand não é definido: o CMD do Dockerfile (entrypoint.py) é usado.
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
